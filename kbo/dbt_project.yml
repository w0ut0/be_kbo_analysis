name: 'kbo'
version: '1.0.0'

profile: 'kbo'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets: 
  - "target"
  - "dbt_packages"

vars:
  kbo_files_root_path: "{{ env_var('HOME') }}/projects/be_kbo_analysis"

models:
  kbo:
    +dbt-osmosis: "_{model}.yml"
    001_staging:
      +materialized: table
